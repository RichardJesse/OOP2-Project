<?xml version="1.0" encoding="UTF-8"?>

<project name="OOP2-Project" default="default" basedir=".">
    <description>Builds, tests, and runs the project OOP2-Project.</description>
    <import file="nbproject/build-impl.xml"/>
    <project name="MyProject" basedir="." default="compile">

    <property name="src.dir" value="src"/>
    <property name="build.dir" value="build"/>
    <property name="lib.dir" value="lib"/>

    <path id="classpath">
        <fileset dir="${lib.dir}">
            <include name="**/*.jar"/>
        </fileset>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="compile" depends="clean">
        <mkdir dir="${build.dir}"/>
        <javac srcdir="${src.dir}" destdir="${build.dir}" classpathref="classpath"/>
    </target>

    <target name="run" depends="compile">
        <java classname="Main" classpath="${build.dir}:${classpath}"/>
    </target>

</project>


    
</project>
